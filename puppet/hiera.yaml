---
:backends:
  - eyaml
  - yaml
:yaml:
  :datadir: "/etc/puppet/hiera/"
:eyaml:
  :datadir: "/etc/puppet/hiera"
  :gpg_gnupghome: "/var/lib/puppet/.gnupg"
  :gpg_always_trust: true
:hierarchy:
  - "%{::server_type}"
  - "%{module_name}"
  - common


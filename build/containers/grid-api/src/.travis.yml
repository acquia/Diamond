language: go
sudo: required
services: docker
go:
  - 1.5
before_install:
  - sudo apt-get update
  - go get github.com/mattn/gom
script:
  - pushd $TRAVIS_BUILD_DIR
  - make docker-build
  - make test-container
notifications:
  email: false
